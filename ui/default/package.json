{
  "name": "default",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.2.1"
  },
  "scripts": {
    "mockdb": "cd ../../inspector && python3 generate_mock_data.py",
    "dev": "concurrently \"cd app && yarn dev\" \"cd server && yarn dev\"",
    "install": "(cd app && yarn) && (cd server && yarn)",
    "clean": "(rm -rf node_modules) && (cd app && rm -rf node_modules) && (cd server && rm -rf node_modules)",
    "clean:db": "(rm ../../inspector/configs.db) && (rm ../../inspector/configs.db-shm) && (rm ../../inspector/configs.db-wal) && (rm ../../inspector/network_traffic.db) && (rm ../../inspector/network_traffic.db-shm) && (rm ../../inspector/network_traffic.db-wal) && (rm ../../inspector/network_traffic2.db)",
    "client:build": "(cd app && yarn build) && (ls && mv app/dist html/)"
  }
}

